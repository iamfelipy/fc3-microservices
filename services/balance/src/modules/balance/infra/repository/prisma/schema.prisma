// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// model User {
//   id        String   @id @default(uuid())
//   name      String
//   email     String   @unique
//   createdAt DateTime @default(now()) @map("created_at")

//   accounts Account[]

//   @@map("users")
// }

model Account {
  id        String   @id @default(uuid())
  //client    User     @relation(fields: [clientId], references: [id])
  clientId  String   @unique @map("client_id")
  balance   Decimal
  createdAt DateTime @default(now()) @map("created_at")

  //transactionsFrom Transaction[] @relation("FromAccount")
  //transactionsTo   Transaction[] @relation("ToAccount")

  @@map("accounts")
}

// model Transaction {
//   id            String   @id @default(uuid())
//   accountIDFrom Account  @relation("FromAccount", fields: [accountIdFrom], references: [id])
//   accountIdFrom String   @map("account_id_from")
//   accountIDTo   Account  @relation("ToAccount", fields: [accountIdTo], references: [id])
//   accountIdTo   String   @map("account_id_to")
//   amount        Decimal
//   createdAt     DateTime @default(now()) @map("created_at")

//   @@map("transactions")
// }
